[package]
name = "models"
version = "0.1.0"
edition = "2021"

[dependencies]
diesel = { version = "2.2.0", features = ["postgres", "64-column-tables", "sqlite", "postgres_backend"] }
serde_json = "1"
serde = {version = "1", features=["derive"]}
